#include "main.h"
/**
 *  _puts_recursion - is a function that print a sting and return to line
 *  @s: is the pointer
 * Return: the new line
*/

void _puts_recursion(char *s)
{
	_putchar(s[]);
	return ('\n');
}
