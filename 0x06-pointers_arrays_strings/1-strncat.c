#include "main.h"
/**
*/

char *_strncat(char *dest, char *src, int n)
{
	int i, j;

	for (i = 0; dest[i] != '\0', i++)
	;
	for (j = 0; src[n] != '\0', n++)
	j = n
	dest[i + j] = src[j];
	return (dest);

}
