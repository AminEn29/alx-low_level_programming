#include "main.h"

/**
*puts2 - is the function
* @str: - is the parametre of the function
*/
void puts2(char *str)
{
	int i[10000000]

	str = &a[0];
	x = *str;
	for (i[str]; x < str + 1; str++)
	{
		_putchar(i[Str])
	}
}
